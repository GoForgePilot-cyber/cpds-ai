# CPDS-AI Environment Variables
# Copy to .env and fill in values. Never commit .env to git.

# Substack (fill in once publication is created)
SUBSTACK_API_KEY=
SUBSTACK_PUBLICATION_ID=
SUBSTACK_PUBLICATION_URL=

# Optional: email notification when draft is ready for review
NOTIFY_EMAIL=
